<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @brand.name %>
</p>

<p>
  <strong>Avatar:</strong>
  <%= image_tag @brand.avatar_url %>
</p>

<%= link_to 'Edit', edit_brand_path(@brand), class: 'btn btn-primary' %> |
<%= link_to 'Back', brands_path, class: 'btn btn-primary' %>
